# ===========================================
# Configuration Backend - Fichier .env.example
# ===========================================
# Copiez ce fichier vers .env et remplissez les valeurs
# cp .env.example .env

# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/meddatacollect
DB_USERNAME=postgres
DB_PASSWORD=your_database_password_here

# JWT Configuration
# Clé secrète pour signer les tokens JWT (minimum 256 bits / 64 caractères hex)
# Générer avec: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_256_bits_64_hex_characters

# CORS Configuration
# Origines autorisées pour les requêtes cross-origin (séparées par des virgules)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Encryption Configuration (AES-256-GCM)
# CRITICAL: Must be exactly 32 characters (256 bits)
# Générer avec: openssl rand -base64 32 | cut -c1-32
ENCRYPTION_KEY=your_32_character_encryption_key
