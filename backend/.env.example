# Database Configuration
DB_PASSWORD=your_database_password_here

# JWT Configuration
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_256_bits

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Encryption Configuration (AES-256-GCM)
# CRITICAL: Must be exactly 32 characters (256 bits)
# Generate a secure key: openssl rand -base64 32 | cut -c1-32
# Or use: python -c "import secrets; print(secrets.token_urlsafe(24)[:32])"
ENCRYPTION_KEY=MySecureEncryptionKey12345678901
